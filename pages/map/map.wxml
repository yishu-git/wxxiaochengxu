<map
id="myMap"
      style="width: 100%; height: 300px;"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      scale
      show-location='true'
>
</map>
<button bindtap="dingwei">定位</button>